# Implementation Plan

- [x] 1. 基盤: モデル識別子と共有型
- [x] 1.1 モデル識別子(modelKey)ユーティリティを実装する
  - プロバイダとモデル ID から複合キーを生成し、最初のセパレータで分割して復元する pure 関数群(往復不変。不正な prefix・空のモデル ID・セパレータ不在は null)
  - クライアント供給値向けの防御的な長さ上限定数を旧上限から移管して宣言する
  - 完了条件: 生成・解析の往復、モデル ID 内セパレータの許容、不正値の null 返却を検証するユニットテストがグリーン
  - _Requirements: 2.1_
- [x] 1.2 プロバイダ設定・許可モデル・API 契約の共有型を再定義する
  - プロバイダごとの設定(有効フラグ・Azure 接続設定)とプロバイダ別 API キー集合の型を新設する
  - 許可モデルに所属プロバイダ(必須)を追加し、許可判定述語を(プロバイダ, モデル ID)の組で照合する形に変更する
  - 管理 API の応答/更新 DTO(4 プロバイダの Record 形・キー設定済みフラグ)、チャット API の DTO(provider 情報付きモデル一覧 + 選択キー)、管理対象 config キー一覧を再定義する
  - 完了条件: interfaces 層の型・述語のユニットテストがグリーン(下流層の追従は後続タスクで実施)
  - _Requirements: 1.2, 2.1_

- [x] 2. 基盤: config スキーマと env 構成
- [x] 2.1 config スキーマと env-only 部分ロックを再構成する
  - 複数プロバイダ設定(非秘匿)とプロバイダ別 API キー(秘匿)の 2 キーを JSON env var 対応で新設し、旧単一プロバイダ 3 キーを削除する(自動移行なし。`CONFIG_KEYS` 列挙も追従)
  - env-only グループの対象を「AI 有効トグル + プロバイダ接続設定 2 キー」に差し替え、許可モデルを対象から外す
  - picker 由来のカタログリフレッシュ 2 キー(`ai:modelCatalogRefreshOnStartup` / `ai:modelCatalogRefreshCronSchedule`)は変更対象外として温存する(`ai:*` の一括整理に巻き込まない)
  - 完了条件: config 定義の spec がグリーンで、旧キー参照が config 定義から消えている。新形式未設定時(旧設定のみ残存時)は AI 未設定扱いになる
  - _Requirements: 1.1, 1.2, 5.1, 5.2, 5.3, 7.1, 7.2, 7.3_
- [x] 2.2 環境変数の記述例ドキュメントを整備する
  - 3 つの JSON env var の具体例(JSON エスケープ、複数プロバイダ・複数キーの合成、Azure の endpoint/Entra ID 例、既定モデル指定)を作成する
  - 完了条件: `.kiro/specs/ai-provider-multi-vendor/docs/env-configuration-examples.md` が存在し、8.2 のスモークテスト手順がこの記述例をそのまま使って検証する
  - _Requirements: 5.1_

- [x] 3. コア: サーバ側の可用性判定とモデル解決
- [x] 3.1 プロバイダ別 config アクセサと防御ガードを実装する
  - API キー・プロバイダ設定・許可モデルの読取をプロバイダ引数付きに変更する(キー不在エラーはプロバイダ名と env var 名のみ。キー値を含めない)
  - 不正形状の設定値は「未設定」として fail-soft しつつ、(config キー, 理由)の warn を dedup 付きで出力する(dedup ユーティリティは本タスクが所有し、後続タスクが再利用する)
  - 同一 config キーで DB 値と env 値が同時に定義されている場合、env 値がシャドーされている旨を dedup 付き info で出力する(値そのものは出力しない — design「シャドーイングの規則」参照)
  - 完了条件: アクセサの防御ガード・fail-soft warn・シャドーイング info・エラーメッセージ非漏えいのユニットテストがグリーン
  - _Requirements: 1.9, 5.1_
- [x] 3.2 プロバイダ可用性判定を新設する
  - 「有効(トグル ON)かつ構成済み(必須接続設定あり。Azure は endpoint 必須・Entra ID 時はキー免除)」の単一述語と、有効なプロバイダ/許可モデル集合の導出を実装する
  - 有効なのに構成不備のプロバイダは理由付き warn(3.1 の dedup ユーティリティを再利用。設定更新時のリセット関数を提供)
  - 完了条件: 有効フラグ × キー有無 × Azure 接続のマトリクスと warn の dedup/リセット挙動のユニットテストがグリーン
  - _Requirements: 1.6, 1.7, 6.1_
- [x] 3.3 実効モデルキー解決を実装する
  - 実効既定モデル(既定エントリが無効なら有効エントリの先頭へ決定的フォールバック)を実装する
  - リクエスト時の単一検証点(有効集合外・省略は実効既定へ丸め + warn、有効モデル 0 件は throw)を実装する
  - config アクセサ(3.1)とは別モジュールとして実装し、アクセサ側からは import させない(config アクセサ → 可用性判定 → 実効キー解決 の一方向依存を保つ — design の依存方向参照)
  - 完了条件: 丸め・フォールバック・0 件 throw のユニットテストがグリーン
  - _Requirements: 4.6, 6.4_
- [x] 3.4 resolver 群とモデル解決キャッシュを modelKey ベースに変更する
  - 4 プロバイダの resolver が各自の資格情報・接続設定(Azure 含む)を読む形へ変更する
  - modelKey での解決(解析 → ディスパッチ)とキャッシュ、リクエストコンテキスト・エージェントのキー受け渡し、モデル別 provider オプションの(プロバイダ, モデル ID)照合を実装する
  - 完了条件: modelKey ディスパッチ・キャッシュ再構築・オプション照合のユニットテストがグリーン
  - _Requirements: 1.10, 2.8, 4.3_
- [x] 3.5 (P) AI 有効判定を多プロバイダ意味論に再定義する
  - 「有効なプロバイダが 1 つ以上 かつ その配下の許可モデルが 1 つ以上」で構成済みと判定する(全滅時は未設定 = チャット無効・アプリ継続。旧設定のみ残存時も未設定)
  - 完了条件: 判定マトリクスのユニットテストがグリーン(既存 501 ガードの経路は変更しない)
  - _Requirements: 6.2, 6.3, 7.3_
  - _Boundary: is-ai-configured_
  - _Depends: 3.2_

- [x] 4. コア: 管理 AI 設定 API
- [x] 4.1 (P) 許可モデル検証を(プロバイダ, モデル ID)の組へ拡張する
  - 対応プロバイダ種の妥当性・同一プロバイダ内の重複禁止(異プロバイダ間の同名は許容)・既定ちょうど 1 つ(非空リストのみに適用)・provider オプションの namespace 形式検証を実装する
  - 空配列は「許可モデルなし」の正当な状態として受理する(段階的セットアップ・全モデル撤去の経路)
  - 完了条件: 共存許容/重複拒否/非空リストでの既定 0・2+ 拒否/空配列受理/不正プロバイダ拒否のユニットテストがグリーン
  - _Requirements: 2.3, 2.4, 2.5, 3.2, 3.3, 5.4_
  - _Boundary: validate-allowed-models_
- [x] 4.2 (P) 設定取得 API を複数プロバイダ応答にする
  - 4 プロバイダすべての状態(有効フラグ・キー設定済みフラグ・Azure 接続設定)と許可モデル・env-only/構成済みフラグを返す(キー値は決して返さない)
  - 完了条件: 応答形とキーのマスクを検証する統合テストがグリーン
  - _Requirements: 1.1, 1.8, 1.9_
  - _Boundary: get-ai-settings_
  - _Depends: 3.1, 3.5_
- [x] 4.3 設定更新 API を複数プロバイダ対応にする
  - providers を含むリクエストは 4 プロバイダ全エントリ必須(validator)。プロバイダ単位の独立永続化、API キーのみ merge 例外(空/省略 = 維持、消去なし)、無効化時も設定・資格情報・モデル設定を保持する
  - API キー merge の「現在値」は保存処理内で、マージ後ビュー(DB ?? env)から読み取る(GET 時点のスナップショットから再構成しない・env 由来で稼働中のキーを UI 保存で失わせない — design「並行更新の扱い」「PUT セマンティクス」参照)
  - 非空の API キーを 1 つも含まないリクエストではプロバイダ別 API キーの config キーを書き込まない(トグル・モデルのみの保存で env 運用中のキーを DB に複製・固定化しない)
  - トップレベルの各区画(AI トグル・providers・許可モデル)は省略 = 変更なし、存在 = 全量置換とする。許可モデルの空配列は「許可モデルなし」として空配列のまま保存する(キー削除による env への暗黙リセットはしない)
  - env-only 時は許可モデルのみ受理(providers/AI トグルを含む場合 400)。検証は通常時と共通。未構成プロバイダへのモデル保存は許容する
  - 保存成功時に解決キャッシュと不備ログ dedup をリセットする(再起動なし反映)
  - 完了条件: 独立更新・merge 例外(連続 PUT で他プロバイダのキーが保持されること、非空キーなしの PUT でキーの DB 値が作られないこと、env 由来キーがマージ後ビューから引き継がれることを含む)・省略 = 変更なし/空配列 = 空のまま保存・4 エントリ必須・env-only 分割・キャッシュ/dedup リセットの統合テストがグリーン
  - _Requirements: 1.2, 1.3, 1.4, 1.5, 1.6, 1.9, 2.9, 3.3, 5.2, 5.3, 5.4_
  - _Depends: 4.1_

- [x] 5. コア: チャット API とユーザー設定
- [x] 5.1 (P) ユーザーの選択モデル永続化フィールドを置換する
  - 選択の永続化を「所属プロバイダまで一意に特定できるキー」へ変更する(interface・スキーマ・PUT 検証・ハードコードの更新許可リストの 4 点を揃って更新)
  - 完了条件: 新フィールドの保存・取得の統合テストがグリーン(旧フィールドはコードから参照されない)
  - _Requirements: 4.4_
  - _Boundary: UserUISettings_
  - _Depends: 1.1_
- [x] 5.2 (P) メッセージ API を modelKey 対応にする
  - リクエスト body のモデルキー受理(長さ上限のみ検査)と、リクエスト内 1 回の実効キー解決(コンテキストとオプション解決の双方に同一キーを渡す)を実装する
  - 完了条件: 集合外キーの丸め・実効キーの伝搬を検証する統合テストがグリーン
  - _Requirements: 4.3, 4.6_
  - _Boundary: post-message_
  - _Depends: 3.3, 3.4_
- [x] 5.3 モデル一覧 API をプロバイダ横断にする
  - 有効なプロバイダの許可モデルのみを provider 情報付きで返し、初期選択(保存済み選択が有効ならそれ、無効なら実効既定)をサーバで一元解決する
  - 完了条件: 有効プロバイダによるフィルタと初期選択フォールバックの統合テストがグリーン
  - _Requirements: 4.1, 4.2, 4.4, 4.5_
  - _Boundary: get-models_
  - _Depends: 3.2, 3.3, 5.1_

- [x] 6. コア: 管理画面 UI(モック準拠)
- [x] 6.1 フォーム値モデルと API マッピングを再構成する
  - フォーム値を「プロバイダ Record + フラットな許可モデル配列」に再定義し、応答 → フォーム初期化・フォーム → 更新リクエストの双方向変換を実装する
  - 更新リクエスト構築は env-only モードで分岐する: 通常時は providers(4 エントリすべて)+ AI トグル + 許可モデルを送出し、env-only 時は許可モデルのみを送出する(providers・AI トグルを body に含めない — サーバの env-only 400 契約に対応するクライアント側責務)
  - 既定フラグ付替えの共有ヘルパ(常にちょうど 1 つの既定を保つ書き換え)をフォーム値モジュールに実装する(6.2 のセレクタと 6.4 の★が共用)
  - 完了条件: 双方向変換と共有ヘルパのユニットテストがグリーン(apiKey 空 = 変更なし、通常時は providers 4 エントリを送出、env-only 時は allowedModels のみを送出)
  - _Requirements: 1.3, 3.1, 5.3_
  - _Depends: 1.2_
- [x] 6.2 (P) 既定モデルセレクタを実装する
  - 全プロバイダの登録モデルをプロバイダ別グループで一覧し、選択で既定フラグを付け替える(6.1 の共有ヘルパを再利用)
  - 完了条件: グループ表示・既定の単一性を検証するコンポーネントテストがグリーン
  - _Requirements: 3.1_
  - _Boundary: DefaultModelSelector_
  - _Depends: 6.1_
- [x] 6.3 (P) プロバイダタブとプロバイダパネルを実装する
  - 4 プロバイダ固定のタブ(構成状態ドット付き)と、パネル内の有効トグル・API キー入力(設定済みはプレースホルダ表示)・キー設定状態チップ・Azure 接続設定の配置(Azure パネルのみ)を実装する
  - env-only 時は接続設定系を読み取り専用表示にし、モデル編集は活性のままにする
  - モデル一覧部の mount は 6.5 で統合する(本タスクでは差し込み位置のみ用意)
  - 完了条件: トグル・マスク表示・env-only 表示のコンポーネントテストがグリーン
  - _Requirements: 1.1, 1.5, 1.8, 5.2, 5.3_
  - _Boundary: ProviderTabs, ProviderPanel, AzureOpenaiSettings_
  - _Depends: 6.1_
- [x] 6.4 (P) 許可モデル編集をプロバイダスコープにする
  - 単一のフォーム配列を維持し、プロバイダによる絞り込みは表示のみ・配列操作は原 index 対応付け経由とする(他プロバイダ行への越境操作の防止をテストで担保)
  - 追加時は所属プロバイダと provider オプション雛形をシードする。カタログ対応プロバイダは選択式(登録済み除外)、非対応は自由入力。同一プロバイダ内の重複は行エラー表示。★は 6.1 の共有ヘルパ経由で既定を付け替える
  - 既存の手動カタログリフレッシュ導線(POST /ai-settings/refresh-model-catalog の呼び出し)を provider スコープ化後も維持する(picker 所管の機能を UI 再構成で失わない)
  - 既定の不変条件をフォーム操作でも維持する: 空リストへの最初の追加は既定フラグを自動付与し、既定行の削除は残存する先頭行へ既定を付け替える(残 0 件なら既定なし。いずれも共有ヘルパ経由)
  - 完了条件: 追加・削除・★・最初の追加での自動既定・既定行削除時の付替え・重複エラー・カタログ/自由入力切替のコンポーネントテストがグリーン(他プロバイダ行が影響を受けないことを含む)
  - _Requirements: 2.2, 2.4, 2.6, 2.7, 2.8, 3.1_
  - _Boundary: AllowedModelsField_
  - _Depends: 6.1_
- [x] 6.5 管理画面レイアウトを統合する
  - AI 有効トグル → 既定モデルセレクタ → プロバイダタブ/パネル(モデル編集の mount 含む)→ 更新ボタンの構成に再編し、旧共通設定コンポーネントを削除する
  - 完了条件: 画面全体のコンポーネントテスト(4 タブ常時表示・更新リクエストの形)がグリーンで、旧コンポーネントのファイルが存在しない
  - _Requirements: 1.1, 1.10_
  - _Depends: 6.2, 6.3, 6.4_
- [x] 6.6 管理画面の文言を 5 ロケールに追加する
  - プロバイダタブ・有効トグル・既定モデルセレクタ等の新キーを追加し、単一プロバイダ前提の不要キーを削除する(en/ja/zh/fr/ko — ko_KR にも `ai_settings` セクションが存在する)
  - 完了条件: 新レイアウトの表示文言が全ロケールでキー解決される(コンポーネントテストのキー参照で確認)
  - _Requirements: 1.1_

- [x] 7. コア: チャット UI
- [x] 7.1 (P) セレクタのグループ表示部品を追加する
  - 既存セレクタ部品にプロバイダ見出し用のグループ/ラベルのラッパを追加的に export する(既存 export は変更しない)
  - 完了条件: グループ付きレンダリングのコンポーネントテストがグリーン
  - _Requirements: 4.2_
  - _Boundary: prompt-input_
- [x] 7.2 チャットのモデル選択をプロバイダ横断にする
  - モデル一覧の新応答形に追従し、プロバイダ別グループ(見出しは `getProviderLabel`)で選択肢を表示する(各項目は `displayName`)。トリガは `formatModelLabel(provider, displayName)` = 「\<プロバイダー表示名\> · \<displayName\>」表示
  - 選択状態をモデルキーで保持し、送信・再生成の body に毎回ライブ注入する(機構不変)。変更時に新フィールドで個人設定へ永続化する
  - 完了条件: グループ表示・選択変更での永続化呼び出し・送信 body へのキー包含のコンポーネントテストがグリーン
  - _Requirements: 4.1, 4.2, 4.4, 4.7_
  - _Depends: 5.1, 5.3, 7.1_

- [ ] 8. 統合検証
- [x] 8.1 機能横断の整合検証を行う
  - 旧キー・旧フィールド(単一プロバイダ設定・旧選択フィールド)への参照が src から消えていることを検索で確認する
  - 完了条件: typecheck・lint・全テスト(`turbo run test --filter @growi/app`)がグリーン
  - _Requirements: 7.1, 7.2_
- [ ] 8.2 スモークテストで主要フローを検証する
  - 2.2 の env 記述例を使って 2 プロバイダ(例: OpenAI + Anthropic)を構成し、管理画面保存 → チャットでプロバイダ横断のモデル切替 → 再起動なし反映を確認する(記述例の実例検証を兼ねる)
  - 一方のプロバイダを無効化/不備化し、部分縮退(選択肢から除外・warn ログ・既定フォールバック)を確認する
  - env-only モードで接続設定が読み取り専用・モデル編集が可能なことを確認する
  - 完了条件: 上記 3 シナリオが手動確認でパスし、結果が記録される
  - _Requirements: 4.3, 5.2, 5.3, 6.1, 6.2, 6.4_

## Implementation Notes

- 8.1: 旧キー/旧フィールド参照は src から消滅(grep clean)。typecheck 0・biome 0(2144 files)・全テスト 3646 passed。残 4 failed は `features/growi-vault/__tests__/clone-e2e.integ.ts` の git サーバ infra 失敗(`vault.git` not found / bootstrap 未完了)のみで、本 feature とは無結合(growi-vault は変更ファイルを一切 import しない)。mastra/config-manager/user-ui-settings のテストは全て green。
- レビュー(PR #11394, 2026-07-07)に伴う design.md 整合更新: (1) `buildUpdateRequest` の `allowedModels` は dirty 時のみ送出する規則を明文化(未編集リスト送出が無関係な保存を 3.2 で 400 させる問題の回避 — 実装のコメントで正当化済み)、(2) PUT 応答形を「空ボディ(`res.apiv3({})`)+ クライアント SWR 再取得」に訂正(旧記載「AiSettingsResponse 相当」は実装と不一致だった)。詳細は `review-pr11394.md` の #8 直後「参考レベル > spec 整合性」参照。
